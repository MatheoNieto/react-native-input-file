import {StyleSheet} from "react-native";

export const Styles = StyleSheet.create({
	button: {
		padding: 16,
		width: '100%',
		borderRadius: 10,
		marginVertical: 4,
		flexDirection: 'row',
		alignItems: 'center',
		justifyContent: 'center',
	},
	labelButton: {
		fontSize: 16,
		textAlign: 'center',
		marginHorizontal: 4,
	}
})
